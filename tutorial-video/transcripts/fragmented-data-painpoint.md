# FragmentedDataPainpoint Transcript

This composition includes generated narration and on-screen text. No critical
meaning depends on audio alone.

## Narration Transcript

One assessment file. One population sheet. One capacity update. And a meeting
that cannot wait. The problem is not only building a chart. The problem is
knowing whether the chart can be trusted. Dashboard Copilot starts with
synthetic fragmented data, profiles evidence, makes gaps visible, and keeps
joins reviewable before anything becomes a dashboard. The handoff is clearer
evidence, attached caveats, and human-owned action.

## On-Screen Transcript

Part 1 / Problem.

The hardest part is knowing whether the chart can be trusted.

One assessment file, one population sheet, one capacity update, and a meeting
that cannot wait.

Mismatched districts. Missing capacity. Late updates.

Part 1 / Proof.

Show the fragmentation before solving it.

The public demo uses bundled synthetic fragmented files, so viewers can test the
workflow without sensitive data.

Click: Use fragmented demo data needs + population + capacity.

Synthetic sample data only. No account or API key is needed.

Part 1 / Evidence.

Make uncertainty visible early.

Before recommendations, the app profiles fields, missingness, inferred roles,
and evidence coverage.

Read: Evidence coverage.

The uncertainty becomes visible before the dashboard exists.

Part 1 / Review.

Do not silently combine fragile data.

Join suggestions and cleaning stay reviewable, row-preserving, and explainable.

Click: Accept recommendation.

No hidden row deletion, imputation, fuzzy matching, or recoding.

Part 1 / Handoff.

Clearer evidence. Human-owned action.

The handoff summary keeps caveats, assumptions, and transformation history
attached for human review.

Click: Generate handoff summary.

Clearer evidence. Human-owned action.
