Data table with hairline horizontal rules only (no vertical lines), sticky panel-tinted header, and a soft hover tint per row.

```jsx
<DataTable
  columns={["Column", "Type", "Missing", "Unique"]}
  rows={[["need_severity_score", "number", "0%", "5"]]}
/>
```
