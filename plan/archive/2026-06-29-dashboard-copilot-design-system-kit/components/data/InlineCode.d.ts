/** Monospace chip for data terms (column/dataset names): #f1f2ed bg, 1px line border, 5px radius. */
export interface InlineCodeProps {
  /** The data term, e.g. "need_severity_score" or "needs_assessment.csv" */
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
