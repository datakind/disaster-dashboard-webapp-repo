KPI tile with muted label and large number. Lay out in `grid-template-columns: repeat(auto-fit, minmax(165px, 1fr))` with 16px gap.

```jsx
<Metric label="Total records" value="2,332" />
<Metric label="Average of Need severity score" value="70" />
```
