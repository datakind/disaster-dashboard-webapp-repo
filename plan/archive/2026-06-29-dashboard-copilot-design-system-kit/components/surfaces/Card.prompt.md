The base white card. Borders over shadows: 1px line + a hairline `0 1px 0` shadow only.

```jsx
<Card eyebrow="CSV" title="needs_assessment.csv">
  <p style={{ color: "var(--muted)", margin: 0 }}>5 rows, 14 columns</p>
</Card>
```
