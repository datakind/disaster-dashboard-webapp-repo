/** Workflow step heading: eyebrow ("Step 3") + Title Case h2, hairline underline. */
export interface SectionHeadingProps {
  eyebrow?: React.ReactNode;
  title: React.ReactNode;
  style?: React.CSSProperties;
}
