Progressive-disclosure accordion built on native `<details>`, with the brand's typographic `+` / `−` marker (no chevron icons) and an optional meta pill.

```jsx
<Accordion summary="Review join/combine recommendation" meta="96% match rate">
  …details content…
</Accordion>
```
