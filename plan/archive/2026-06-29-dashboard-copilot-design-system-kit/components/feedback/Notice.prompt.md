Amber warning / red error block with soft tinted background. Copy is calm and factual — explain the fallback, never blame the user.

```jsx
<Notice tone="warn" items={["AI is off. Deterministic profiling, evidence coverage, readiness checks, and dashboard recommendations remain available."]} />
<Notice tone="error" items={["One or more files could not be loaded. Check the files and try again."]} />
```
