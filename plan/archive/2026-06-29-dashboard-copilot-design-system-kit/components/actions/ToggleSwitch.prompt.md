Pill-shaped switch with inline state copy ("AI **On**") and a sliding thumb; the track fills teal when checked.

```jsx
<ToggleSwitch label="AI" checked={llmEnabled} onChange={setLlmEnabled} />
```

Used in the sticky app header. Keep labels to one short word.
