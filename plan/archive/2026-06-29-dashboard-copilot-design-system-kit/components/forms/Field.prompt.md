Labeled text input — muted 0.86rem label (weight 720) stacked 6px above a 40px white control with `--line-strong` border.

```jsx
<Field label="Decision-maker" value={v} onChange={setV} placeholder="Provincial response coordinator" />
```

Siblings: `SelectField`, `TextAreaField`, `CheckboxRow`. Lay fields out in CSS grids with 14–16px gap.
