Labeled textarea, vertical resize only — used for decision questions and semantic notes.

```jsx
<TextAreaField label="Semantic comment for the agent" rows={3} placeholder="Rows are districts; severity_score is 0-100." />
```
