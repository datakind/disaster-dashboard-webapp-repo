Checkbox with inline label, 9px gap — evidence choices and export acknowledgements.

```jsx
<CheckboxRow label="I reviewed the decision-readiness caveats before exporting." checked={ok} onChange={setOk} />
```
