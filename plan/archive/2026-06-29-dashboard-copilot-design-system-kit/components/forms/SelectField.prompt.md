Labeled select with the brand's CSS double-triangle chevron (no icon font). Options are strings or `{value,label}`.

```jsx
<SelectField label="Join or ID field" value={col} onChange={setCol} options={["Not selected", "admin_pcode", "district_name"]} />
```
